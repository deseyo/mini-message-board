<html>
  <head>
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
    <ul>
      <% for (let i=0; i < messages.length; i++) { %>
          <li>
            <div><%= messages[i].user %></div>
            <div><%= messages[i].text %></div>
            <div><%= messages[i].added %></div>
            <a href="/<%= i %>">Message details</a>
          </li>
      <% } %>
    </ul>
    <span><a href="/new">New message</a></span>
  </body>
</html>